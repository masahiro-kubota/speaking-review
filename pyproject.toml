[project]
name = "speaking-review"
version = "0.1.0"
description = "PoC scripts for reviewing English speaking sessions"
readme = "docs/specification.md"
requires-python = ">=3.10"
dependencies = [
    "fastapi>=0.136.1",
    "uvicorn>=0.46.0",
]

[tool.uv]
package = false
